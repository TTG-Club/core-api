package club.ttg.dnd5.domain.bastion.player.model;

/** Состояние сооружения персонажа. */
public enum FacilityStatus {
    /** Готово и принимает приказы. */
    READY,
    /** Строится: базовое сооружение, купленное после закладки. */
    BUILDING
}
